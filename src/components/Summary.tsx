// Remove or comment out the unused variables
// const { searchParams } = useSearchParams();
// const { removeAll } = useCart();

// If these variables are needed later, consider using them or prefixing with an underscore
// const { searchParams: _searchParams } = useSearchParams();
// const { removeAll: _removeAll } = useCart();
